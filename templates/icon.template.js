module.exports = (
  version,
  date,
  iconComponent,
  svg,
  config
) => `// Generated by util/generate-icons.js v.${version}
// on ${date}.
import * as React from "react";
import { IconSvgProps } from "../Icon.types";

const ${iconComponent}Icon: React.FC<IconSvgProps> = ({
  size = ${config.jsx.defaultProps.size},
  height,
  width,
  color = ${config.jsx.defaultProps.color},
  strokeWidth = ${config.jsx.defaultProps.strokeWidth},
  className${config.jsx.defaultProps.className.length > 0 ? ` = "${config.jsx.defaultProps.className}",` : ","}
  ${config.jsx.restProps ? "...rest" : ""}
}: IconSvgProps) => {
  return (
    ${svg}
  );
};

export default ${iconComponent}Icon;
`;
